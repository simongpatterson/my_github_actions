name: Simple Greeting

# 1. When should this run?
on: [push]

# 2. What should it do?
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo "Hello, GitHub Actions! Your code was pushed."
